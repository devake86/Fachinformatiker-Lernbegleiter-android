<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="AnswerButton" parent="Widget.AppCompat.Button">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:gravity">center</item>
        <item name="android:ellipsize">end</item>
        <item name="android:singleLine">true</item>

    </style>

    <style name="ActionButton" parent="Widget.AppCompat.Button">
        <item name="android:layout_width">174dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:gravity">center</item>
        <item name="android:ellipsize">end</item>
        <item name="android:singleLine">true</item>

        <item name="backgroundTint">?attr/colorCustomButton</item>
        <item name="android:textColor">?attr/colorOnCustomButton</item>

    </style>

</resources>